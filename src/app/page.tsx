import { Header } from "@/sections/header";
import { HeroSection } from "@/sections/hero";

const Home = () => {
  return (
    <div>
      <Header />
      <HeroSection />
    </div>
  );
};

export default Home;
